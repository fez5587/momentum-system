apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "momentum.configMapName" . }}
  labels:
    {{- include "momentum.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
